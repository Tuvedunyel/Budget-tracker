import { render } from "@testing-library/react"
import Details from "../components/Details/Details"

describe('Test on Details', () => {
    it('Details is rendered', () => {
        render(<Details />)
    })
})